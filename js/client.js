function leaveRequest(){
    //var registerUsername= document.getElementById("startDate").value;
    //var registerPassword= document.getElementById("endDate").value;
    var reason= document.getElementById("reason").value;
    var address= document.getElementById("address").value;
    if (
        //startDate!='' && endDate!= '' && 
        reason!= '' 
        && address!='' ) {
       
    }

    else{

        alert('all fields are required');

    }
}
